// Técnica:

// 1) Observe o trecho de código abaixo: 

const indice = 13 
const soma = 0
const letra = 0

function enquanto(letra, indice, soma) {
      if (letra < indice){
            return letra = letra + 1 && soma === soma + letra
      }
}
enquanto()
console.log(`O resultado da soma é: ${soma}`);